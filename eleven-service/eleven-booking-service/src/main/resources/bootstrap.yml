server:
  port: 9001

spring:
  application:
    name: @artifactId@
  profiles:
    active: @facility@
  datasource:
    url: ${db.booking.url}
    username: ${db.booking.username}
    password: ${db.booking.password}
  cloud:
    nacos:
      config:
        enabled: ${ENABLE_NACOS:false}
        group: ${NACOS_GROUP}
        namespace: ${NACOS_NS}
        server-addr: ${NACOS_HOST}
      discovery:
        enabled: ${ENABLE_NACOS:false}
        group: ${NACOS_GROUP}
        namespace: ${NACOS_NS}
        server-addr: ${NACOS_HOST}

  liquibase:
    database-change-log-table: 001_DATABASECHANGELOG
    database-change-log-lock-table: 001_DATABASECHANGELOGLOCK

  jpa:
    generate-ddl: true
    properties:
      'hibernate.order_updates': true
      'hibernate.jdbc.batch_size': 50

  data:
    mongodb:
      uri: ${mongo.booking.uri}

eleven:
  cache:
    duration: PT8H
  data:
    id-type: raindrop
  time:
    provider: local_clock
  logs:
    logstash:
      enabled: false
  web:
    open-api:
      title: Booking API
      description: ross hotel management tool
      version: 1.0.0.dev
      terms-of-service: https://www.demcia.com/
      contact:
        email: demcia@demcia.com
        name: 德玛西亚小分队
        url: https://www.demcia.com/
      license:
        name: Apache 2.0
        url: https://www.demcia.com/


