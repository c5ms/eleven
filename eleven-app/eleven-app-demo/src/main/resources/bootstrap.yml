spring:
  cloud:
    nacos:
      config:
        enabled: ${ENABLE_NACOS}
        group: ${NACOS_GROUP}
        namespace: ${NACOS_NS}
        server-addr: ${NACOS_HOST}
      discovery:
        enabled: ${ENABLE_NACOS}
        group: ${NACOS_GROUP}
        namespace: ${NACOS_NS}
        server-addr: ${NACOS_HOST}
